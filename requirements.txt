# ─────────────────────────────────────────────────────────────────────────────
# AI Meeting Assistant — requirements.txt
# Python 3.11 required (3.12 works; 3.13+ NOT supported by PyTorch/ChromaDB)
#
# INSTALL ORDER — run these commands in sequence:
#
#   1. Fix build tooling first (prevents pkg_resources / setuptools errors):
#      uv pip install --upgrade setuptools wheel pip
#
#   2. Install PyTorch CPU-only (skip CUDA unless you have an NVIDIA GPU):
#      uv pip install torch torchaudio --index-url https://download.pytorch.org/whl/cpu
#
#   3. Install ffmpeg via winget (run once in any terminal, outside the venv):
#      winget install --id=Gyan.FFmpeg -e
#      Then restart your terminal so ffmpeg is on PATH.
#
#   4. Install everything else:
#      uv pip install -r requirements.txt
# ─────────────────────────────────────────────────────────────────────────────

# ── Build tooling (must be present before Whisper builds from source) ─────────
setuptools>=69.0.0
wheel>=0.43.0

# ── Audio / Video Download ────────────────────────────────────────────────────
yt-dlp>=2024.4.9
pydub>=0.25.1
ffmpeg-python>=0.2.0

# ── Transcription ─────────────────────────────────────────────────────────────
# Use latest Whisper — older pins (20231117) fail with "No module named pkg_resources"
# because their setup.py predates the setuptools split. Latest build is cleaner.
openai-whisper==20250625

# ── Explicitly block the bad llvmlite that Whisper's dep chain can pull in ────
llvmlite>=0.43.0
numba>=0.59.0

# ── Translation (Hindi → English) ────────────────────────────────────────────
deep-translator>=1.11.4

# ── LLM / LangChain LCEL ─────────────────────────────────────────────────────
langchain>=0.2.0,<0.4.0
langchain-community>=0.2.0,<0.4.0
langchain-mistralai>=0.1.6,<0.3.0
mistralai>=1.0.0,<2.0.0

# ── Embeddings (HuggingFace) ──────────────────────────────────────────────────
sentence-transformers>=2.7.0,<4.0.0
huggingface-hub>=0.23.0,<1.0.0

# ── Vector Store — ChromaDB ───────────────────────────────────────────────────
chromadb>=0.5.0,<0.7.0
langchain-chroma>=0.1.1,<0.3.0

# ── PyTorch — installed separately in step 2 above ───────────────────────────
torch>=2.2.0,<2.5.0
torchaudio>=2.2.0,<2.5.0

# ── Streamlit UI ──────────────────────────────────────────────────────────────
streamlit>=1.35.0,<2.0.0

# ── PDF Export ────────────────────────────────────────────────────────────────
reportlab>=4.2.0,<5.0.0

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv>=1.0.1
tiktoken>=0.7.0,<1.0.0
numpy>=1.26.0,<2.0.0
tqdm>=4.66.0